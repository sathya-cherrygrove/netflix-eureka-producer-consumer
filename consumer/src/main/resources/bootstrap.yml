server:
  port: 8081
management:
  security:
    enabled: false
spring:
  application:
    name: consumer
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-service-discovery:8090/eureka